workspace {
    cmdline += -include $PENGUIN_ROOT/MPC/config
    cmdline += -feature_file $PENGUIN_ROOT/MPC/config/penguin.features
    
    $(ACE_ROOT)/ace
    $(PENGUIN_ROOT)
    
    exclude {
        ext
    }
}